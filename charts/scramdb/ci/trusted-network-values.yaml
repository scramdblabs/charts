# No authentication anywhere, for a network already under your control. Both the
# client port and the MCP tool surface stay on a ClusterIP.
architecture: standalone
auth:
  enabled: false
mcp:
  auth: env
service:
  type: ClusterIP
  exposeMcp: true
persistence:
  enabled: false
